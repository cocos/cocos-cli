# CLI SDK 与 Engine SDK 拆分方案

CLI SDK 和 Engine SDK 独立打包、发布和升级。CLI 按兼容范围选择引擎，支持项目指定自定义 Engine SDK；PinK 接入不在该方案范围内。

## 版本与兼容性

兼容策略由 CLI 根目录的 `engine-compatibility.json` 声明：

- 正式版 `~4.0.0`：接受 `>=4.0.0 <4.1.0`。
- 过渡开发版：接受 `4.0.0-alpha.N`，N ≥ 32。
- 后继开发版：接受 `4.1.0-alpha.N`，N ≥ 0，并提示实验性支持。
- 未声明的预发布系列、非法版本及范围外正式版均拒绝使用。

版本匹配后继续检查必需文件、配置内容和运行时接口。可选能力通过实际接口判断；缺少可选功能不应影响已有功能。

## 引擎选择

路径优先级为命令行 / API 显式路径、项目 `engineSdk.path`、CLI 本机 `config.local.json` 的 `enginePath`、CLI 目录下的 `packages/engine`。

显式路径无效时直接报错。项目相对路径以项目根目录为基准，本机相对路径以 CLI 根目录为基准。项目可以通过 `engineSdk.version` 和 `engineSdk.revision` 锁定精确身份；路径覆盖不能绕过身份校验。一个进程只使用一份引擎。

`version` 表示引擎 SemVer；可选 `revision` 表示精确修订。源码引擎可以没有 revision，打包 SDK 按文件清单生成内容摘要。元信息不使用 distribution 字段。

## 安装与打包

`npm ci` / `npm install` 安装 CLI 依赖，不自动编译引擎。源码环境通过 `fetch:engine`、`install:engine`、`setup:dev` 显式准备；已有 Engine SDK 时使用 `setup:cli`。

`pack:cli-sdk` 与 `pack:engine-sdk` 分别生成准备好的目录快照。打包不隐式安装、编译或发布。CLI SDK 不包含引擎；Engine SDK 包含运行所需代码、声明、适配器、工具和依赖。产物记录平台、Node ABI 和文件摘要。

`cocos doctor --json` 提供只读诊断。成功初始化后记录项目实际使用的引擎身份，不改写项目锁定条件。

## 全版本测试

从 `cocos/cocos4` 枚举全部匹配 tag，固定引擎 commit 及历史 `native/external-config.json` 指定的 external commit，在独立目录准备 Engine SDK。正式 `4.0.0` tag 出现前，额外保留 `v4.0.0` 分支基线；基线保留实际版本号。

每个 CLI / Engine 组合独立执行身份与接口检查、项目导入和 Web 构建、完整 unit、MCP 类型生成及完整 E2E。下载失败、缺少版本、空结果、失败或超时均使矩阵失败。Unit 失败后仍执行 E2E，保留完整诊断信息。

PR 验证候选提交；定时任务按维护列表验证 CLI ref。报告记录 ref 快照、SDK 摘要及逐组合结果。通过的候选可交给独立 SDK 发布流程；既有发布入口的统一门禁需另外接入。

后续完整 ZIP 可替代源码准备阶段，继续使用相同的校验与测试流程。版本矩阵覆盖已定义用例，项目运行、设备、渲染和性能回归需要代表性项目测试。

## 操作与验证

- [环境配置](docs/dev/environment-setup.md)
- [独立 SDK 打包](docs/sdk-packaging.md)
- [Git tag 测试矩阵](docs/sdk-tag-matrix.md)
- [完整矩阵验证结果](docs/sdk-full-ci-results.md)
